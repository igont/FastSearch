---
TDR: TDR-FS-2.1; TDR-FS-2.6; TDR-FS-3
Индексация: true
---
# FastSearch как поисковый контур

## Назначение и исходное положение

FastSearch является локальным инструментом поиска по допущенной документации и исходному коду. Он находит и упорядочивает фрагменты источников, а FastGraph отдельно владеет структурой связей, ревизиями графа и оценкой влияния.

## Границы ответственности

FastSearch владеет каталогом рабочих областей, допуском корней, безопасным чтением, исходными снимками, точным, полнотекстовым и векторным поиском, получением содержимого и состоянием поисковых проекций.

FastSearch не формирует полный список связанных документов с пояснениями и не выдаёт поисковую запись за графовый факт. Такой результат может быть добавлен отдельной композицией с FastGraph без изменения базового поискового ответа.

## Входы и результаты

Пользователь или агент передаёт рабочую область, один поисковый запрос и при необходимости фильтры состояния проекта и статуса документа. Без параметров фильтрации поиск охватывает все допущенные записи.

Обычный семантический поиск возвращает до шести уникальных фрагментов в едином порядке релевантности. Каждый результат содержит номер, заголовок, путь, состояние проекта, статус документа и найденное содержимое. Модели, их исходные позиции и внутренние оценки в публичный ответ не входят.

Для FastGraph отдельный поставляющий порт возвращает точный снимок уже допущенных исходных единиц. Он не публикует внутренние поисковые сегменты и не определяет графовую идентичность.

## Взаимодействие

Один и тот же результат обычного поиска доступен человеку в консоли, оператору в JSON и агенту через MCP. Эти представления строятся из общего типизированного ответа и не меняют порядок или смысл найденных фрагментов.

FastGraph потребляет снимок через версионированный контракт и самостоятельно строит граф. Dtree и другие агенты получают поисковые кандидаты от FastSearch, а графовое расширение - от FastGraph через отдельные порты.

## Отказы и восстановление

Обычный семантический поиск выполняется только при готовности закреплённого поискового набора моделей и относящихся к нему проекций. При неполной готовности запрос не выдаёт упрощённый или частичный ответ под видом обычного результата. После восстановления артефактов и проекций тот же публичный контракт становится доступен без смены команды.

Явно выбранные точный, полнотекстовый и структурный режимы сохраняют собственные состояния доступности. Они не используются как скрытая замена неготового семантического поиска.

## Инварианты

- Обычный поиск возвращает один общий порядок из не более чем шести уникальных фрагментов.
- Номер результата означает место по релевантности и не является оценкой актуальности документа.
- Поисковая запись не является графовым фактом.
- FastSearch не читает и не изменяет физическое хранилище FastGraph.
- Допуск пути и безопасное чтение имеют одного владельца в FastSearch.
- Отсутствие фильтра не исключает документы по состоянию проекта или статусу документа.
- Публичный ответ не раскрывает внутреннее голосование поисковых моделей и оценку переранжирования.

## Последствия

Агент получает один устойчивый сценарий: передать запрос и прочитать до шести результатов без выбора моделей и разбора нескольких несопоставимых выдач. Консоль и MCP остаются представлениями одного поискового результата, а расширение связанными документами не связывает базовый поиск с готовностью FastGraph.

## Связи и основания

[TDR-FS-2.1](<../../Docs/TDR/TDR-FS-2.1 Каталог и admission рабочей области.md>) определяет допуск рабочей области. [TDR-FS-2.6](<../../Docs/TDR/TDR-FS-2.6 Единый поиск и переранжирование.md>) задаёт фиксированный поисковый набор, метаданные, единый порядок и публичный ответ. [TDR-FS-3](<../../Docs/TDR/TDR-FS-3 Поставщик снимков источников.md>) определяет поставляющий контракт для внешнего потребителя.

[Передача графового контура](<../../Docs/FastGraph.md>) закрепляет FastGraph владельцем полного графа и расширенных связей.

## Проверка соблюдения

Граница соблюдена, если один и тот же запрос без выбора модели возвращает семантически одинаковые шесть лучших фрагментов через консольный JSON и MCP, а отсутствие FastGraph не меняет этот ответ. При неготовности обязательного поискового набора проверка должна получить явную ошибку готовности, а не частичную выдачу.

## Отложенные вопросы

Пакетная выдача связанных документов с краткими пояснениями рассматривается после появления соответствующего контракта FastGraph.

## Термины

Переранжирование - повторная оценка общего набора найденных кандидатов одной моделью для построения итогового порядка. Поисковая проекция - воспроизводимое представление источников для одного способа поиска.

