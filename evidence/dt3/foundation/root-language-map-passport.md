# Root, language and map passport

Accepted named roots: `document-representative`, `code-fastsearch`. Rust `.rs` and Python `.py` are the only structural admission candidates; parser output means structural symbol/locator, never compiler definition/reference. Syntax error, duplicate names and Unicode locator remain explicit partial/error paths for D1. Tree-sitter is roadmap authority; its crate/grammar version is intentionally deferred to D1 bounded evidence, so no silent parser substitute is allowed.

Fixtures cover AUTO, CURATED, stale, change/delete target and invalid mode. Schema is not accepted by A1: C owns eventual map schema/lifecycle; `.cfmap.md` stays one source-classification, not Markdown admission.
