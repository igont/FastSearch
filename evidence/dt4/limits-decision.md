# Решение G-LIMITS

- Дата: `22.08.2026`.
- Производящая редакция A2: `12ed53bc60a3ae2db328078741fb1cc6148e73a8`.
- Источник измерений: `evidence/dt4/resource-preflight.json`.
- Состояние: `ACCEPTED`.
- Confidence: `medium` - обязательные роли измерены на целевой Windows-машине, но полный сквозной проход A5 ещё не выполнен.

## Принятые пределы

| Параметр | Предел | Худшее успешное наблюдение | Запас и причина | Публичный исход |
|---|---:|---:|---|---|
| Длина запроса | `1024` Unicode scalar values | `1024` символа для каждой из трёх проекций | это наибольший измеренный вход; более длинный текст не квалифицирован | `INVALID_REQUEST`, `retryable: false` |
| Срок внешнего поиска | `30 s` | до `2.956 s` холодной проекции; Qwen `1.702 s` открытия и `3.193 s` для 10 пар | консервативная последовательность трёх параллельных проекций и Qwen для 15 кандидатов укладывается примерно в `9.5 s`; предел даёт более чем трёхкратный запас на I/O и планировщик | `TIMEOUT`, `retryable: true` |
| Сериализованный ответ | `131072 B` (`128 KiB`) | `65559 B` для полезной нагрузки `64 KiB` | почти двукратный запас; публичный ответ ограничен шестью записями, превышение не усекать | `RESPONSE_TOO_LARGE`, `retryable: false` |
| Пиковый рабочий набор процесса | `8589934592 B` (`8 GiB`) | консервативная сумма пиков Arctic, E5 и Nomic `6733967360 B` | около `27.6%` над суммой трёх отдельных пиков; Qwen наблюдался отдельно на `2496540672 B` и не может пересекаться с проекциями | `SEARCH_FAILED`, `retryable: true` |
| Обязательный свободный остаток | `4294967296 B` (`4 GiB`) | минимально наблюдавшаяся свободная память до модельной пробы превышала `18 GiB` | защищает ОС и соседние процессы от исчерпания памяти | `NOT_READY`, `retryable: true` |
| Свободная память перед поиском | `12884901888 B` (`12 GiB`) | во всех пробах доступно более `18 GiB` | сумма бюджета `8 GiB` и обязательного остатка `4 GiB`; проверяется до постановки запроса в работу | `NOT_READY`, `retryable: true` |

## Окна приёмочных проверок

| Проверка | Окно | Основание |
|---|---:|---|
| MCP initialize и protocol-only ответ | `10 s` | удваивает использованное в preflight окно быстрого протокольного обмена |
| EOF после закрытия `stdin` | `10 s` | сервер обязан завершиться без модельной работы и постороннего `stdout` |
| Ответ `BUSY` третьему вызову | `2 s` | очередь уже занята и модельный этап для решения не требуется |
| Подавление ответа отменённого вызова | `35 s` | срок поиска `30 s` плюс `5 s` на освобождение непрерываемого этапа и управляемое закрытие |
| Один холодный сквозной поиск A5 | `30 s` | совпадает с внешним сроком поиска; превышение закрывает проход без частичной выдачи |

## Кандидатный и публичный пределы

По управляющей корректировке `MGMT-2` каждая из трёх моделей эмбеддингов возвращает до пяти кандидатов. До удаления повторов существует ровно 15 входных слотов; после удаления повторов Qwen ранжирует уникальные записи, а публичный ответ содержит не более шести.

## Сравнение профилей

- Принятый сбалансированный профиль - `+8` по влиянию, `3/10` по реализации, confidence medium: сохраняет измеримый запас и реальную защиту машины.
- Узкий профиль `15 s / 64 KiB / 7 GiB` - `+4`, `4/10`, confidence medium: усиливает ограничения, но слишком близок к одиночной машине и создаёт ложные отказы.
- Широкий профиль `60 s / 1 MiB / 12 GiB` - `+3`, `2/10`, confidence medium: проще пройти, но он поздно обнаруживает зависание и почти не ограничивает расход памяти и ответ.

Пределы не являются результатами быстродействия для других машин. A5 обязан проверить их на полном маршруте; любое превышение возвращает типизированную ошибку без частичного списка и закрывает `G-RESOURCE@A5`.
